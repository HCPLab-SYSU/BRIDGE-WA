# Runtime dependencies for Bridge-WA policy evaluation on VLABench.
#
# VLABench's upstream requirements.txt also installs data conversion, training,
# notebook, and alternate-policy stacks. Those packages are not needed by the
# HTTP evaluator and make the simulator environment much harder to reproduce.

# Simulator versions used by the vendored VLABench revision.
numpy==1.25.0
scipy==1.14.0
mujoco==3.2.2
dm-control==1.0.22
gym==0.26.2
mediapy==1.2.0
open3d==0.18.0
opencv-python==4.10.0.84
h5py==3.11.0

# Dependencies imported while VLABench registers tasks and evaluators.
colorama==0.4.6
colorlog==6.9.0
gdown>=5,<7
imageio>=2.34,<3
ipdb>=0.13,<1
json-numpy==2.1.0
networkx>=3.3,<4
openai>=1.0
pillow>=10,<12
PyYAML>=6,<7
requests>=2.31,<3
scikit-learn>=1.5,<2
tqdm>=4.66,<5
