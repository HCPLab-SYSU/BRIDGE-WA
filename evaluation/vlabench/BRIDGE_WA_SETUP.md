# Bridge-WA VLABench Notes

Use `evaluation/README.md` as the main setup guide.

The local VLABench code is included, but large assets must be downloaded with:

```bash
bash scripts/download_vlabench_assets.sh
```

Bridge-WA evaluation entry:

```bash
bash scripts/eval_bridge_wa_vlabench.sh
```
