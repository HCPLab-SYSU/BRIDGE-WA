# Bridge-WA Evaluation on VLABench

Use the release entry from the repository root:

```bash
MODEL_PATH=./checkpoints/bridge_wa_vlabench \
VLABENCH_CONDA_ENV=vlabench \
SERVER_CONDA_ENV=bridge-wa \
bash scripts/eval_bridge_wa_vlabench.sh
```

Install the simulator and assets as described in `../README.md`.
